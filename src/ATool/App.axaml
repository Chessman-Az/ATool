<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ATool"
             x:Class="ATool.App"
             RequestedThemeVariant="Dark">

  <Application.DataTemplates>
    <local:ViewLocator />
  </Application.DataTemplates>

  <Application.Resources>
    <!-- 科幻深色色板：深空蓝黑底 + 电光青蓝主色 -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#38BDF8" />
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#0EA5E9" />
    <SolidColorBrush x:Key="PrimarySoftBrush" Color="#14314F" />
    <SolidColorBrush x:Key="BgBrush" Color="#070D18" />
    <SolidColorBrush x:Key="CardBrush" Color="#0F1B2E" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E2EAF6" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#7E93AD" />
    <SolidColorBrush x:Key="BorderBrush" Color="#1E3A5C" />
    <SolidColorBrush x:Key="DangerBrush" Color="#F87171" />
    <SolidColorBrush x:Key="SuccessBrush" Color="#34D399" />
    <SolidColorBrush x:Key="WarningBrush" Color="#FBBF24" />
    <CornerRadius x:Key="CardRadius">12</CornerRadius>
    <Thickness x:Key="CardPadding">16</Thickness>
    <!-- 覆盖 Fluent 系统强调色（选中态/焦点/accent 按钮跟随）——注意：Fluent 期望 Color 类型 -->
    <Color x:Key="SystemAccentColor">#38BDF8</Color>
    <Color x:Key="SystemAccentColorDark1">#0EA5E9</Color>
    <Color x:Key="SystemAccentColorDark2">#0284C7</Color>
    <Color x:Key="SystemAccentColorDark3">#0369A1</Color>
    <Color x:Key="SystemAccentColorLight1">#7DD3FC</Color>
    <Color x:Key="SystemAccentColorLight2">#BAE6FD</Color>
    <Color x:Key="SystemAccentColorLight3">#E0F2FE</Color>
  </Application.Resources>

  <Application.Styles>
    <FluentTheme />

    <!-- ============ 全局控件样式（科幻深色风） ============ -->

    <Style Selector="Window">
      <Setter Property="Background" Value="{DynamicResource BgBrush}" />
    </Style>

    <Style Selector="TextBlock.h1">
      <Setter Property="FontSize" Value="18" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
    </Style>
    <Style Selector="TextBlock.h2">
      <Setter Property="FontSize" Value="14" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
    </Style>
    <Style Selector="TextBlock.hint">
      <Setter Property="FontSize" Value="11" />
      <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
    </Style>
    <Style Selector="TextBlock.danger">
      <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
    </Style>

    <Style Selector="Button">
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="Padding" Value="14,7" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Background" Value="{DynamicResource CardBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
    </Style>
    <Style Selector="Button:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource PrimarySoftBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
    </Style>
    <Style Selector="Button.accent">
      <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
      <Setter Property="Foreground" Value="#04121F" />
      <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
    </Style>
    <Style Selector="Button.accent:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource PrimaryHoverBrush}" />
    </Style>
    <Style Selector="Button.danger">
      <Setter Property="Background" Value="{DynamicResource DangerBrush}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}" />
    </Style>

    <Style Selector="TextBox">
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="Padding" Value="10,7" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
    </Style>
    <Style Selector="TextBox:focus /template/ Border#PART_BorderElement">
      <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
    </Style>

    <Style Selector="ComboBox">
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="FontSize" Value="13" />
    </Style>
    <Style Selector="NumericUpDown">
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="FontSize" Value="13" />
    </Style>
    <Style Selector="DatePicker">
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="FontSize" Value="13" />
    </Style>

    <Style Selector="ListBox">
      <Setter Property="Background" Value="Transparent" />
    </Style>
    <Style Selector="ListBoxItem">
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="Margin" Value="2,2" />
      <Setter Property="Padding" Value="12,9" />
    </Style>
    <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource PrimarySoftBrush}" />
    </Style>
    <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="#152741" />
    </Style>

    <Style Selector="TabItem">
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Padding" Value="18,10" />
      <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
    </Style>
    <Style Selector="TabItem:selected">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style Selector="Expander">
      <Setter Property="CornerRadius" Value="8" />
    </Style>

    <Style Selector="CheckBox">
      <Setter Property="FontSize" Value="13" />
    </Style>
    <Style Selector="RadioButton">
      <Setter Property="FontSize" Value="13" />
    </Style>
  </Application.Styles>

  <!-- 系统托盘：左键显隐、右键菜单（显示/刷新/退出）。Command 绑定 Application.DataContext（App.axaml.cs 设置） -->
  <TrayIcon.Icons>
    <TrayIcons>
      <TrayIcon Icon="/Assets/icon.ico"
                ToolTipText="A工具"
                Command="{Binding ToggleWindowCommand}">
        <TrayIcon.Menu>
          <NativeMenu>
            <NativeMenuItem Header="显示主窗口" Command="{Binding ShowWindowCommand}" />
            <NativeMenuItem Header="立即刷新余额" Command="{Binding RefreshBalanceCommand}" />
            <NativeMenuItemSeparator />
            <NativeMenuItem Header="退出程序" Command="{Binding QuitCommand}" />
          </NativeMenu>
        </TrayIcon.Menu>
      </TrayIcon>
    </TrayIcons>
  </TrayIcon.Icons>
</Application>
